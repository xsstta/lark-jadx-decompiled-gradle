package com.bytedance.ee.bear.browser.demo;

import com.bytedance.ee.util.io.NonProguard;

public class WebData implements NonProguard {
    public static boolean validate(WebData webData) {
        return webData != null;
    }
}
