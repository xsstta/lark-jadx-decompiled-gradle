package com.ss.android.lark.mail.impl.p2168g.p2169a;

/* access modifiers changed from: package-private */
/* renamed from: com.ss.android.lark.mail.impl.g.a.ab */
public class C42113ab {
    /* renamed from: a */
    static boolean m168091a(String str) {
        if (String.class.getName().equals(str) || "long".equals(str) || Long.class.getName().equals(str) || "int".equals(str) || Integer.class.getName().equals(str) || "char".equals(str) || Character.class.getName().equals(str) || "float".equals(str) || Float.class.getName().equals(str) || "double".equals(str) || Double.class.getName().equals(str) || "boolean".equals(str) || Boolean.class.getName().equals(str) || "short".equals(str) || Short.class.getName().equals(str) || "byte".equals(str) || Byte.class.getName().equals(str)) {
            return true;
        }
        return false;
    }
}
